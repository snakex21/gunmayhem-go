# Reference fonts

These font exports are kept with the original/reference material but are not used by the current Go runtime.

The font files required by the current HUD/menu implementation live in `assets/fonts/`. Keeping the remaining exported fonts here preserves the reference material without mixing unused files into the runtime asset tree.
